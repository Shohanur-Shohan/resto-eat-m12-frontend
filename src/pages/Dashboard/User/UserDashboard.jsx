import { Helmet } from "react-helmet-async";

const Dashboard = () => {
  return (
    <div>
      <Helmet>
        <title>Resto Eat | Dashboard</title>
      </Helmet>
      user dashboard
    </div>
  );
};

export default Dashboard;
